[build]
  # python (auto-detected by Railway)
  [build.nixpacks]
    aptPackages = []
    # Railway auto-detects Python + FastAPI, no extra config needed

[deploy]
  startCommand = "uvicorn app:app --host 0.0.0.0 --port $PORT"
